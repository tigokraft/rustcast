[workspace]
resolver = "2"
members = [
    "core-proto",
    "remote-server",
    "shell/src-tauri",
    "system-bridge",
    "mobile-remote/src-tauri",
]

[workspace.dependencies]
core-proto = { path = "core-proto" }
